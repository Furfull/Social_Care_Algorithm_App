import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    logo: {
        alignItems:"center",
      },
      stretch: {
        width: 120,
        height: 120,
        resizeMode: 'stretch',
        alignItems:"center",
      }
})

export default styles