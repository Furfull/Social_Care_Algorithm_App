import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    boxTitle: {
        alignItems:"center",
    },
    textTitle:{
        color: "#000000",
        fontSize:18,
        alignItems:"center",
        textAlign:"center",
        fontWeight:"bold",
        width:"95%",
        paddingTop:"3%"}
})

export default styles